# STATUS — 프로젝트 현재 상태 (인수인계 문서)

> 최종 갱신: 2026-07-26 (v1.61) · **네이버 돌파** · 응답 구조 조사 · 텔레그램 명령 + 월 필터 · 노선 10개 · 피치 제외 · 노선 11개 · 콤보 1,468 · 안정 운영 중

### 2026-07-26 04:07 안정성 점검 (v1.44 반영 후)
- 732건 검색 · 실패 0 · 콤보 1,468 (교차 543)
- 데이터 신선도: 6시간 이내 1,243 / 24시간 이내 191 / 오래됨 71
- 1인 가격 130,200 ~ 1,971,338 (중앙값 532,383) · **이상치 0건**
- 누적 알림 151건에서 증가 없음 → 중복 억제 정상 작동
- 도시별 콤보: 도쿄 386 · 나고야 356 · 오사카 344 · 후쿠오카 91 ·
  오키나와 89 · 삿포로 86 · 제주 81 · 가고시마 35
- 시간 분포 5회 누적(표본 5배)으로 재검증 — 넓힌 창 3곳 결론 동일 · 운영 중 · 노선 9개 전부 가동 · 새 대화에서 이어갈 때 이 문서와 SPEC.md를 먼저 볼 것

## 프로젝트 한 줄 요약
항공권 최저가를 GitHub Actions(공개 저장소 `hminhoz/flight-price-alert`)에서
자동 감시, 특가 시 텔레그램 알림. 설계·결정 근거는 SPEC.md.

## 현재 상태: **알림 개시** (v1.22에서 관측 4일→2일로 단축)
first_run 2026-07-24 기준 관측은 7/24~25로 종료. **7/26(KST)부터 특가 알림 작동.**
기준가가 이틀치 관측으로 만들어져 다소 거칠지만, 기준가 = 관측 최저가라
알림이 남발되는 구조는 아니다. 운영하며 필요하면 config에서 다시 늘릴 것.

### 워크플로우 파일은 v1.6 이후 변경 없음
cron·타임아웃만 들어 있고 나머지는 전부 config.yaml에서 조절한다.
샤드 수·알림 정책 변경에 워크플로우 수정이 필요 없다 (2026-07-26 기준 원격과 동일 확인).

### 실행 모드 3가지 (Actions → Run workflow → dry_run 입력칸)
| 입력값 | 동작 |
|--------|------|
| (빈칸) | 실전. 예약 실행과 동일 |
| `1` | 테스트. 텔레그램 전송·상태 저장 모두 없음 |
| `preview` | 미리보기. 기준가 무시하고 도시별 최저 3건씩 알림 형식으로 전송, 저장 없음 |
| `digest` | **지금 최저가 한 통. 도시별 1줄씩 요약, 저장 없음** |
`preview`는 "알림이 어떻게 오는지 지금 보고 싶다"용. 워크플로우 파일을 고치지
않으려고 기존 dry_run 입력값을 재활용했다.

- 파이프라인 전 구간 작동: 자동 실행(cron 매시 시도, 커서 샤딩) → 검색 →
  가격 수집 → 콤보 76개 → 텔레그램 전송
### 노선 현황 (2026-07-26 기준)
**9개 노선 전부 가동 확인** (2026-07-26 실측, 205 leg 실행):
| 노선 | 가격확보 | 노선 | 가격확보 |
|------|---------:|------|---------:|
| 인천-나리타 | 32 | 인천-나고야 | 18 |
| 김포-하네다 | 25 | 인천-오키나와 | 18 |
| 인천-오사카 | 22 | **김포-제주** | **15** |
| 인천-후쿠오카 | 20 | 인천-가고시마 | 14 |
| 인천-삿포로 | 17 | | |
합계 205 시도 → 가격확보 181 · 조건불일치 18 · 실패 0 · 데이터없음 6

**v1.10의 "구글에 한국 국내선 데이터 없음" 판정도 오진이었다.** 제주는 잘 된다.
남은 휴면: 인천-하네다(경유편만 반환) · 인천-제주(김포-제주 안정되면 켤 것)

1회 실행 248 leg · 예상 13~15분 (제한 75분)
- v1.12는 기준가 리셋이 필요 없는 설계 → **7/28 알림 시작 일정 그대로 유지**

## 확정된 진단 (재조사 불필요)

### 2026-07-25 1차
1. fast-flights(v3.0.2)는 구글 원본 시각을 [시, 분] 숫자 배열로 넘김 (0시는 null).
   v1.7에서 파싱 수정 후 가격 수집 정상화.
2. **오사카(KIX)·후쿠오카(FUK)·나리타(NRT)**: 3단 폴백 모두 오류 → 구글 소스 불가 확정.
3. **인천-하네다**: 경유편만 반환 → 사실상 불가. 김포-하네다가 도쿄를 커버.
4. **국내선(제주)**: 구글에 한국 국내선 데이터 자체가 없음. 네이버 소스 필수.
5. GitHub 무료 예약 실행 지연/생략 → 커서 샤딩으로 대응 완료.

### 2026-07-25 2차 (data/ 실측 분석으로 발견, v1.11·v1.12로 대응)
6. **시간창이 노선을 죽이고 있었음**: 콤보 76개 중 75개가 3개 노선에 몰려 있었다
   (하네다 31 · 나고야 22 · 오키나와 22 / 삿포로 1 · 가고시마 0).
   원인은 스크래퍼가 아니라 전역 시간창 대 실제 운항 스케줄 —
   ICN→KOJ는 출발편이 오후 편중(12:47·15:45), CTS→ICN은 저녁 출발편이 없음.
   → v1.11 노선별 시간창 override로 해결. SPEC §3.1.
7. ~~**가격 척도가 왜곡돼 있었음**~~ **(8번에서 뒤집힘 — 아래 먼저 볼 것)**: 가격 끝자리가 방향별로 100% 갈림.
   가는 편은 원화 원본(793,200), 오는 편은 엔화 환산(1,021,430).
   일본 출발 편도 요금이 비싸 오는 편 중앙값이 가는 편의 약 2배 → 편도 합산가가
   실구매가보다 크게 높다(나고야 9월 기준가 81만 vs 한국 발권 왕복 실가 50~70만대).
   → v1.12에서 **감지 지표(편도 합산) / 표시 금액(왕복 실가) 분리**. SPEC §6.4.
   판정은 기존 척도 유지 → 기준가 리셋 불필요.

8. **편도 2장 vs 왕복 티켓은 노선마다 정반대다 (7번 전제 폐기)**.
   2026-07-25 노선별 DRY_RUN 실측 (모두 왕복 응답 구간수=1, 파서 정상):

   | 노선 | 편도 2장 | 왕복 티켓 | 결과 |
   |------|---------:|---------:|------|
   | GMP-HND 8/08~12 | 1,774,759 | 1,169,800 | 왕복 -34% |
   | ICN-OKA 9/09~13 | 1,067,380 |   801,400 | 왕복 -25% |
   | ICN-KOJ 10/10~13|   974,753 |   892,000 | 왕복 -8%  |
   | ICN-CTS 9/11~15 | 1,179,247 | 1,257,800 | 편도 -7%  |
   | ICN-NGO 8/20~24 |   731,370 | 1,165,000 | 편도 -59% |

   2026-07-26 재측정(다른 날짜 조합): 하네다 -34% · 가고시마 -20% · 나고야 -17%
   · 삿포로 -3% (왕복 우세) / 오키나와 +4% (편도 우세). **나고야가 뒤집혔다**
   → 노선뿐 아니라 날짜 조합마다도 갈린다. 둘 다 병기하는 v1.14 설계가 타당.
   → 어느 한쪽을 대표 금액으로 고정하면 절반의 경우 비싼 값을 알리게 된다.
     v1.14에서 **두 금액을 나란히 싣고 싼 쪽을 자동 강조**하도록 확정.
   → 직전 v1.13의 "편도 2장이 항상 싸다"는 나고야 3건만 본 표본 편향이었음.
   → 판정·기준가는 계속 편도합산 척도 (리셋 불필요, 변동 감지에는 충분).

9. **시간창이 실제로 돈을 먹고 있다 (미해결)**. RTDIAG의 왕복 후보 목록에서
   최저가가 오전 출발 조건에 걸려 탈락하는 사례 확인:
   · ICN-NGO 후보 [678800, 698200, 935000, 1059800, 1165000] → 채택 1,165,000
     (최저 678,800은 가는 편 15:05 출발이라 06:00~13:00 밖)
   · GMP-HND 후보 최저 1,084,400(15:45 출발) 탈락 → 1,169,800 채택
   → 오전 출발 고수 비용이 노선당 5만~49만 원.
   → **2026-07-25 사용자 결정: 시간창 유지.** 도착 첫날을 쓰는 것이 그 차액보다
     중요하다는 판단. 재론하지 말 것. (KOJ·CTS의 노선별 override는 운항 스케줄
     자체가 없어서 넣은 것이므로 이 결정과 무관하게 유지)

## v2 네이버 소스 — 중단 결정 (2026-07-26)

3차에 걸친 탐침 끝에 접었다. **같은 시도를 반복하지 말 것.**

| 차수 | 시도 | 결과 |
|------|------|------|
| 1차 | requests로 후보 4종(REST·GraphQL × 국제·국내) | 전부 503, 592바이트 동일 |
| 2차 | 대조군 검증 | naver.com 200 · 항공권 페이지 200 · **API만 503** → IP 차단 아님 |
| 3차 | primp(크롬 TLS 지문) + 세션 쿠키 + HTML 내 API 주소 추출 | A·B 여전히 503 · 추출 결과 0건 |

결론: API 앞단이 브라우저 아닌 접근을 일관되게 거절한다. TLS 지문 우회로도
안 뚫린다. 페이지는 Next.js 껍데기라 HTML에 API 주소가 없다.

미탐색 경로(재도전 시 여기서 시작): JS 번들을 내려받아 실제 엔드포인트를 추출.
단 주소를 찾아도 앞단이 여전히 막을 공산이 크다. 기대값 낮음.

### 스카이스캐너 대안 — 검토 완료, 보류
- 공식 API: 파트너 전용·심사·개인 승인 난망 → 불가
- 중개(RapidAPI Sky Scrapper 등): 무료 티어 월 100회
- **규모**: 휴면 6노선을 왕복 조회로 하루 1회만 훑어도 약 600건/일 = 월 1.8만 건.
  무료 티어와 자릿수 2개 차이 → 유료 필수
- 2개 노선(오사카·후쿠오카)으로 줄이면 약 200건/일 = 월 6천 건
- 판단: 비용을 들일 가치가 있는지는 사용자 결정 사항. 현재 보류

### 휴면 노선 무료 재도전 (v1.19) — v1.10 판정을 뒤집을 근거 발견
"구글 소스 불가"라는 v1.10 판정의 근거가 부실했다. 3단 폴백이 전부 같은 요청
방식이었고 실패 원인을 끝까지 파지 않았다. 2026-07-26 재조사로 셋을 확인:

1. **파서 소스 확인**: 실패 지점은 `parse_js`의 `errorHasStatus: true`.
   파싱 실패가 아니라 **구글이 그 쿼리에 오류 상태를 반환**하는 것.
2. **데이터는 있다**: DIAG 로그의 경유편 샘플에 ICN→KIX(06:55 아시아나),
   ICN→FUK(13:55 아시아나)가 들어 있다. 직접 검색만 실패한다.
3. **안 써본 옵션 발견**: fast-flights 3.0.2 `FlightQuery(airlines=[...])`.

가설: 죽은 3노선(KIX·FUK·NRT)은 취항사 10곳 이상인 초대형 노선이고,
      되는 노선(NGO·CTS·KOJ)은 3~4곳뿐이다. 응답이 커서 오류가 날 수 있다.
      → 항공사 필터로 결과를 줄이면 통과할지 모른다.

#### 탐침 결과와 결론 (2026-07-26)
1차: 우회 6종 전부 실패. 그러나 실패 메시지가 두 종류로 갈렸다.
- `no flights found; received error` = `errorHasStatus: true` (구글이 진짜 오류)
- `list index out of range` = **정상 데이터인데 파서가 죽음**

2차(v1.20 관대 파서 투입) ICN-KIX 9회 시도 내역:
```
A.기본       list index out of range   ← 데이터 있음
A.원본HTML   2,832,880 bytes · errorHasStatus=False  ← 데이터 있음
B/C/D        list index out of range   ← 데이터 있음
E/F/G/G2     errorHasStatus            ← 진짜 오류
```
→ **오사카는 9번 중 5번 정상 데이터가 온다. 간헐적일 뿐 불가가 아니다.**
  v1.10의 "구글 소스 불가" 판정은 틀렸다.

#### 진범: 안 쓰는 필드를 고정 인덱스로 읽는 기본 파서
fast-flights `parse_js`가 `payload[7][1][0]`(항공동맹 메타)와 `flight[22]`(탄소)를
고정 인덱스로 읽는다. **둘 다 이 프로젝트가 안 쓰는 값**인데 취항사 많은 노선에서
구조가 달라 IndexError가 나고 결과 전체가 버려졌다.

#### 대응과 성과
`app/gparse.py`(관대 파서) — 가격·항공사·구간 시각만 방어적으로 추출.
`app/search.py`의 `_run_query`에 폴백 연결. 기본 파서 성공 시엔 건드리지 않고,
`errorHasStatus`면 관대 파서도 None을 주어 기존 재시도·3단 폴백이 유지된다.
복구 시 로그에 `TOLERANT ...` 기록.

**기존 5노선에서도 큰 효과** (2026-07-26 실측, 같은 109 leg 기준):
| | 직전 | v1.20 |
|---|---|---|
| 가격확보 | 79 | **92** |
| 데이터없음 | 19 | **3** |
| 콤보 | 108 | **116** |
ICN-CTS 데이터없음 9→0(가격확보 9→17), ICN-OKA 6→0(13→18).
잘 돌던 노선도 응답의 상당수를 조용히 버리고 있었던 것.

#### v1.21: 오사카·후쿠오카 시험 부활
간헐적으로라도 데이터가 오므로 config에서 주석 해제. 나리타·인천하네다·제주는
휴면 유지(각각 오류 지속 / 경유편만 반환 / 구글에 국내선 데이터 없음).
**몇 번 실행 후 노선별 통계 확인 → 가격확보가 너무 적으면 다시 주석 처리.**

### 무료 트랙 2: Travelpayouts(아비아세일즈) Data API — 미착수
- **무료 등록, 호출당 과금 없음.** prices/calendar 300 RPM (분당!)
  → 스카이스캐너 중개(월 100회)와 비교 불가할 만큼 여유
- 한계: 실시간 아님. 아비아세일즈 사용자 검색 기반 캐시(48시간~7일)
- 리스크: 러시아권 사용자 기반이라 한-일 노선 캐시가 얇을 수 있음. 검증 필요
- 실시간 Search API는 MAU 5만 필요 → 불가. Data API만 쓸 수 있다
- 착수하려면: travelpayouts.com 가입 → 토큰 발급 → GitHub Secret 등록

## v1.12.2 DRY_RUN 결과 (2026-07-25 14:22, 92 leg, 4분)
- 시간창 조정 효과 확인: ICN-KOJ 조건불일치 20→4, ICN-CTS 0, **콤보 105→169개**
- 실패 0건 / DRY_RUN 상태 미저장 정상 동작 확인 ("변경 없음" 커밋)
- 단 이 실행의 수집분은 저장되지 않았으므로 실제 저장된 콤보 수는 아직 105 근처.
  예약 실행이 몇 번 돌면 169 수준으로 올라올 것

## 관찰 대기 항목
- [x] 네이버 3차 탐침 완료 → 포기 확정 (위 표 참고)
- [ ] **실행 몇 번 후 ICN-KIX·ICN-FUK·ICN-NRT·GMP-CJU 노선별 통계** → 유지/재휴면
- [ ] **첫 특가 알림 텔레그램 화면 확인**
- [x] RTDIAG 확인 완료: 구간수 항상 1, 파서 정상 (진단 9번 참고)
- [x] 왕복 검증의 gl=KR 폴백도 작동 확인 (ICN-CTS가 en-US 실패 후 KR로 성공)
- [x] 가는 편 시간창 완화 → 유지하기로 결정 (진단 9번)
- [ ] **DRY_RUN 수동 실행으로 `왕복 검증 사전 점검` 결과 확인** (7/28 전에)
- [ ] v1.12.1 시간창 조정 후 KOJ·CTS 조건불일치가 줄어드는지
- [ ] 7/28 첫 특가 알림에서 `RTVERIFY` 로그 확인 —
      "편도합산 X → 왕복실가 Y (N% 낮음)"가 찍히면 v1.12 성공.
      왕복 조회가 계속 실패하면 알림에 "편도합산 참고치"로 표시됨 → 그때 재설계
- [ ] 가동 2주 후 data/의 daily_min 히스토리로 "원거리 하루 1회" 충분성 검증
- [ ] 대화방 양방향 명령(/exclude 등)은 v2 후보

## 버전 이력 (요약)
- v1.0~1.3 구현·계층화(임박 2h/주시 2회/원거리 1회+시각회전)·편도합산 설계
- v1.4~1.5.2 no-data 분리, 유사가격 섹션, 출발일·요일 제외(일월화 제외 중)
- v1.6 커서 샤딩 + cron 1시간 + 도쿄 공항 분리 + 수집현황 폴백 리포트
- v1.7 시각 [시,분] 배열 파싱 (가격확보 0건 사태 해결)
- v1.8~1.9 3단 폴백(직항→무필터→한국마켓), 직항 재시도, 실패 0 달성
- v1.10 구글 불가 6노선 휴면 처리, 인수인계 문서 확정
- **v1.11 노선별 시간창 override** (가고시마·삿포로 부활)
- **v1.12 왕복 실가 검증** (감지 지표와 표시 금액 분리, 기준가 리셋 없이)
- **v1.12.1 시간창 경계 조정** (KOJ ~17:00, CTS 14:00~) + DRY_RUN 사전 점검
- **v1.61 네이버 돌파 성공 → 응답 본문 직접 수집으로 전환**

### 🎉 4차 결과: 뚫렸다 (2026-07-26, 7번째 시도)
```
2/2 건에서 가격 확보 · 화면 :99(Xvfb)
[201] flight-api.naver.com/flight/domestic/searchFlights       text/event-stream
[201] flight-api.naver.com/flight/international/searchFlights  text/event-stream
본문 510자 → 6,327자 / 24,516자
김포-제주 42,772 47,000 47,870 … · 인천-오사카 62,484 66,396 72,848 …
```
**결정적 요인은 headless 중단 + 실제 화면(Xvfb) + 스텔스 스크립트.**
HTTP(requests·primp)로는 5번을 시도해도 503이었는데, 실제 화면을 띄운
브라우저에서는 페이지가 스스로 검색 API를 호출했다.

### 5차: 화면 긁기 → 응답 직접 받기
정규식으로 화면 글자를 긁으면 그 숫자가 1인 편도인지 총액인지도 모른다.
API 주소를 알아냈으니 **응답 본문을 그대로 받아 구조를 파악**한다.
· `searchFlights` 응답을 `res.text()`로 확보, 앞 1,800자와 data 줄 수 기록
· 실패 대비로 결과 행 하나의 HTML도 함께 남긴다
이걸로 정확한 파서를 짜면 가격·시각·항공사를 구글과 같은 단위로 뽑을 수 있다.

- **v1.60 네이버 탐침 4차 — 가상 화면 + 스텔스**

### 3차로 밝혀진 것 (2026-07-26)
페이지는 **완벽히 준비된다**. 실물 요소 목록에서 확인:
`인천 ICN 인천국제공항 / 오사카 KIX 간사이국제공항 / 09.17.목 / 09.21.월 /
성인 2명 / 직항만`, 그리고 결과 화면 컨트롤(`가격 낮은 순`, `왕복 동시 선택`,
`카드혜택`)까지 렌더링됨. 검색 버튼 실물 = `searchBox_btn_search__LEqDN`.
그런데 **검색 API 호출이 0건**이고 본문은 항상 510자 안팎.
클릭 여부와 무관 → 페이지가 스스로 검색을 걸지 않는다. 자동화 감지로 보인다.

### 중단 기준 재조정 (중요)
"3차에서 안 되면 접는다"는 **얻을 게 있는지 모르는 상태**를 전제로 했다.
2026-07-26 사용자가 **네이버에 실제로 더 싼 게 존재함을 확인**했다.
상금이 실재하므로 기준을 다시 잡는다 — 남은 가장 센 수단까지는 시도한다.

### 4차: 남은 최강 수단
· **headless 중단, 가상 화면(Xvfb)에 실제 화면을 띄운 브라우저**로 전환.
  headless가 가장 알아보기 쉬운 형태다. GitHub 러너는 sudo가 되므로
  xvfb를 직접 설치해 `DISPLAY=:99`로 띄운다. 실패하면 headless로 폴백.
· **스텔스 초기 스크립트** — `navigator.webdriver` 제거, languages·plugins·
  chrome 객체·permissions 보정. 페이지 스크립트보다 먼저 주입.
· `--disable-blink-features=AutomationControlled` 플래그.
· 3차에서 확인한 실물 선택자 `[class*=searchBox_btn_search]`로 정확히 클릭.

**이번에도 검색 API가 0건이면**: 네이버는 자동 수집을 적극적으로 막는다고
결론짓고 **자동화는 접는다.** 대신 알림·고정판의 네이버 링크를 활용한
수동 확인 흐름으로 전환할 것 (이미 링크는 붙어 있다).

- **v1.59 네이버 탐침 3차 — 선택자 추측 중단, 요소 목록 조사**

### 2차 결과: 클릭이 오히려 망가뜨렸다
`button:has-text('검색')`이 **헤더 통합검색 버튼**을 눌러 항공권 페이지에서
튕겨 나갔다. 본문이 1차의 전체 페이지 → **513자 오류 화면**으로 축소
(표식: 검색 결과 / 로그인 / 일시적). 6건 모두 동일.
기록된 요청도 광고·로그(veta·tivan·nlog)뿐, 검색 API는 여전히 0건.

### 3차: 추측 대신 조사
· **아무것도 클릭하지 않는다.** 대신 페이지의 버튼·링크를 실제로 열거해
  텍스트와 class를 목록으로 남긴다 (최대 40개).
· 클릭 없이 결과가 저절로 붙는지 60초 관찰 (1차는 30초였다).
· 시험 대상 6건 → **2건**. 요소 조사가 목적이라 반복할 이유가 없다.

**중단 기준**: 이 3차에서 올바른 버튼을 특정하지 못하면 네이버는 접는다.
지금까지 HTTP 3회 + 브라우저 2회 = 5회 시도했다. 들인 품 대비 회수 가능성을
계속 재평가할 것.

- **v1.58 네이버 탐침 2차 — 검색 버튼 클릭**

### 1차 탐침 결과 (2026-07-26) — 접근은 뚫렸다
- **페이지 정상 로드.** 본문에 "왕복 … 김포 GMP 김포국제공항 제주 CJU"까지
  조건이 채워진 진짜 항공권 페이지. requests/primp가 503이던 것과 완전히 다르다.
  → **브라우저 경로는 유효하다.**
- 다만 가격 0/6. 원인은 API 기록에 드러났다:
  `flight.naver.com/api/runtime-config`(설정)와 `sentry/api/…`(에러 수집)뿐.
  **검색 API 호출이 0건** = 페이지는 열렸지만 검색이 시작조차 안 됐다.
  URL 진입만으로는 조건만 채워지고 검색 버튼을 눌러야 하는 구조로 보인다.

### 2차 보완
· 로드 후 '항공권 검색' 버튼을 찾아 클릭 (선택자 5종 순차 시도)
· 대기 30초 → **60초** (네이버 검색은 10~30초 걸린다)
· 응답 기록에서 URL 키워드 필터 제거 — 1차에서 `api`가 든 것만 봐서
  설정·에러 수집기만 잡혔다. 이미지·JS만 빼고 다 본다
· 실패 시 원인 단서 기록: 버튼을 눌렀는지, 본문 길이, 페이지 표식
  ('검색 결과' / '항공편이 없' / '로그인' / '오류')

- **v1.57 네이버 브라우저 탐침** (`DRY_RUN=naver`)

### 소스 추가 검토 결론 (2026-07-26)
| 소스 | 공식 무료 | 비공식 무료 | 구글과 재고 |
|---|---|---|---|
| 카약 | 제휴 승인 필요 | 유료 중개만(체험 50~1,000건) | 대부분 중복 |
| 스카이스캐너 | 제휴 승인 필요 | 월 100회 → 자릿수 부족 | 대부분 중복 |
| Travelpayouts | ✅ 무료 300RPM | — | **시각·직항·인원 필터 없음 → 탈락** |
| **네이버** | 없음 | API는 503, **브라우저 미시도** | **다름(한국 시장)** |
→ 카약·스카이스캐너는 메타서치라 구글과 같은 OTA 재고를 본다. 붙여도 중복.
  **실질적으로 다른 걸 보는 건 네이버뿐.**

### 붙일지 따로 만들지 — 판단 기준 (재사용할 것)
1. 데이터 단위가 같은가 (노선·방향·날짜별 최저가 1건)
2. 조건 필터가 되는가 (직항·출발 시각·성인 2명) ← Travelpayouts가 여기서 탈락
3. 실행 부담이 같은가 (가벼운 HTTP vs 브라우저) ← 네이버가 여기서 갈림
4. 실패가 격리되는가
→ 네이버는 1·2 통과, 3에서 갈림. **같은 저장소·다른 주기**가 맞다.

### 방식 결정: 상위 후보 재확인 ❌ → 하루 1회 전체 훑기 ✅
재확인은 구글이 이미 찾은 것만 봐서 **새 발견이 0**이다. 전체 훑기는
브라우저 4초/건 × 1,243건 = 순차 83분이지만 탭 3개 병렬이면 약 28분 —
하루 1회면 제한(75분) 안에 들어간다. 데이터가 최대 24시간 묵는 건 감수.

### 탐침 설계
`DRY_RUN=naver` → 검색 건너뛰고, **구글 가격이 이미 있는 날짜쌍 6건**으로 시험.
"뚫리나"와 "구글보다 싼 게 있나"를 한 번에 확인한다. playwright는 실행 시점에
설치(평소 실행을 무겁게 하지 않으려고 requirements.txt에 넣지 않음).
페이지가 호출하는 API 주소도 응답 가로채기로 기록 — 나중에 가벼운 방식으로
옮길 실마리가 된다. 결과는 `data/naver_probe.json`(워크플로우가 data/를
커밋하므로 로그 없이 확인 가능).

- **v1.56 보기 전용 모드는 검색을 건너뛴다**
  `digest`·월 요약은 저장된 데이터를 다르게 그려줄 뿐인데, 그 전에 663건을
  8분간 검색하고 **결과를 저장도 안 한 채 버리고** 있었다.
  → 검색을 건너뛰어 즉시 응답. 구글 요청도 아낀다.
  · 실행 순서를 테스트로 고정했다(검색 생략 → 응답 → 고정판 → 저장).
    순서가 뒤집히면 조회 한 번에 고정판이 덮어써지거나 수집분이 날아간다.

**보기 전용 모드는 아무것도 바꾸지 않는다** (오프라인 실행으로 검증):
`digest` / `8` 은 고정판 갱신·상태 저장 **앞에서 return** 하므로
`data/` 전체가 그대로다. 조회를 몇 번 하든 주기 실행에 영향이 없다.

- **v1.55 깃허브 입력에도 월 지정** — 텔레그램에만 붙이고 깃허브를 안 맞춰서
  같은 기능인데 한쪽에서만 되던 상태였다. 이제 두 경로가 같은 규칙을 쓴다.

### 실행 방법 두 가지 (같은 기능, 즉시성만 다름)
| 하고 싶은 것 | 깃허브 dry_run 입력 | 텔레그램 |
|---|---|---|
| 실전 | (빈칸) | (자동) |
| 테스트 (전송·저장 없음) | `1` | — |
| 전체 자세히 | `digest` | `/digest` |
| 8월만 자세히 | `digest 8` | `/digest 8` |
| 8월만 가볍게 한 통 | `8` 또는 `8월` | `/8` |
| 알림 형식 점검 | `preview` | — |
- 깃허브 = **즉시 실행**. 텔레그램 = 편하지만 **다음 실행까지 최대 1시간**.
- 어느 쪽이든 **새로 검색하지 않는다.** 이미 저장된 데이터를 다르게 보여줄 뿐
  (계산은 1초). 기다림은 검색 때문이 아니라 봇이 메시지를 읽는 시점 때문이다.

- **v1.54 고정판에 명령 안내 한 줄** — 기능이 있는 줄 모르면 안 쓴다.
  고정판 제목의 시각이 갱신됐다면 그 실행에서 명령도 확인된 것이므로,
  "읽고 있는지"를 따로 알릴 필요는 없다.

- **v1.53 월별 조회 (`/8`, `/8월`)**
  · 월만 던지면 **가벼운 한 통**(고정판 형식, 요약)으로 답한다.
    `/digest 8`은 자세히(여러 통, 날짜마다 링크). 훑어보기와 비교하기를 나눴다.
  · 처음엔 월 필터도 digest로 붙였는데 4통 11,662자가 나왔다.
    "가볍게 보고 싶다"는 요청과 정반대라 한 통 요약으로 갈아탐.
  · `format_board` / `format_digest`에 `month` 인자. 해당 월 조합이 없으면
    "N월 출발 조합이 아직 없습니다"로 안내.
  · 실측: 8월 3,794자 · 9월 3,501자 · 10월 3,520자 — 모두 한 통.

### 텔레그램 명령 정리
| 명령 | 결과 |
|---|---|
| `/8`, `/8월` | 그 달 출발만 **가볍게 한 통** |
| `/digest` | 전체를 자세히 (여러 통, 날짜마다 링크) |
| `/digest 8` | 8월만 자세히 |
| `/help` | 안내 |
모두 **다음 실행 때** 처리된다 (최대 1시간). 즉시 보려면 고정판.

- **v1.52 텔레그램에서 명령 받기** (`/digest`, `/help`)
  텔레그램 명령을 받으려면 봇이 상시 켜져 있어야 하는데 무료 구성에는 서버가
  없다. 웹훅도 불가. → **실행할 때마다 `getUpdates`로 밀린 메시지를 훑는다.**
  · 즉시 응답 아님. 다음 실행까지 **최대 1시간** 지연. 안내 문구에 명시했다.
  · `meta.tg_offset`으로 이미 처리한 메시지를 건너뛴다.
  · **허용된 chat_id에서 온 것만** 처리 — 아무나 봇을 불러 조회를 돌리면 안 된다.
  · 실패해도 본 작업(검색·알림)을 막지 않는다.
  · 즉시 응답이 꼭 필요해지면 Cloudflare Workers 같은 무료 웹훅 호스트가
    선택지다. 다만 별도 계정·배포가 필요해 지금 구성보다 손이 많이 간다.

- **v1.51 digest 날짜 수 6개로** — 요청해서 자세히 보는 모드인데 상시 요약인
  고정판(4~5개)보다 적게(3개) 보여주고 있었다. digest는 여러 통으로 나눌 수
  있어 길이 제약이 덜하다. 실측 4통 · 링크 48개.

### 보기 수단 3가지 (역할 구분)
| | 고정판 | `digest` | `preview` |
|---|---|---|---|
| 언제 | 실행마다 자동 | 요청 시 1회 | 요청 시 1회 |
| 통수 | 1통 (내용만 수정) | 여러 통 | 도시마다 1통 |
| 알림 | **안 울림** | 울림 | 울림 |
| 링크 | 도시당 1개 | **날짜마다** | 날짜마다 |
| 날짜 | 도시별 4~5 (자동 맞춤) | 도시별 6 | 알림과 동일 형식 |
| 용도 | 평소 훑어보기 | 자세히 비교 | 알림 형식 점검(주로 개발용) |

- **v1.50 항공사 제외 기능 · 고정판에 항공사 표기** (사용자 요청)
  · `search.exclude_airlines: [MM, PEACH]` — 피치항공 제외.
    2글자는 IATA 코드, 3글자 이상은 항공사명 부분일치로 본다
    (코드가 안 잡히는 응답까지 걸러내려고 이름도 함께 등록).
  · **수집·조합 양쪽에서 차단**한다. 수집만 막으면 이미 저장된 다리가
    `leg_freshness_days`(3일) 동안 남아 설정을 바꿔도 며칠간 계속 노출된다.
  · 시간 분포 집계에서도 제외분은 세지 않는다 — 시간창 판단이 실제로 고를 수
    있는 항공편 기준이어야 하기 때문.
  · 고정판의 '다른 날짜' 줄에도 항공사를 넣었다.

  **파급 (실측)**: 콤보 1,468 → 986. 피치 비중이 컸다.
  | 노선 | 다리 | 결과 |
  |---|---|---|
  | **김포-나고야** | 113 → **0** | 피치 단독 취항 → **노선 비활성화** |
  | 인천-오사카 | 112 → 49 | 피치가 절반 이상이었으나 생존 |
  | 김포-오사카 | 96 → 84 | 생존 |
  가격 영향: 나고야 282,139 → 354,324원/인, 오사카 212,604 → 223,194원/인.
  → 노선 11개 → 10개, 1회 실행 732 → 663건.
  **피치를 다시 허용하려면** `exclude_airlines`를 비우고 config의
  김포-나고야 주석을 풀면 된다.

- **v1.49 고정판 상태를 해시로 축소**
  `meta.json`에 고정판 **전문 4KB를 통째로** 저장하고 있었다. 실행마다 그
  덩어리가 커밋에 쌓인다. 게다가 수정 성공 시 그 값을 갱신하지 않아 비교
  자체가 무용지물이었다.
  → 12자 해시만 보관. 내용이 같으면 **API 호출도 생략**한다.
    예전 `:text` 키가 남아 있으면 자동으로 정리한다. 테스트로 고정.

### 고정판 운영 메모
- `meta.board_ids = {chat_id: message_id, "chat_id:h": 해시}`
- 실행마다 `editMessageText`로 그 메시지를 갱신 → **알림 안 울림, 고정 유지**
- 메시지가 삭제됐으면 자동으로 새로 보내고 id를 갱신한다 (다시 고정 필요)
- 2026-07-26 기준 board_ids = {"495657895": 112}

- **v1.48 고정판 날짜 수 자동 맞춤**
  도시별 1건만 자세히 보여 정보량이 부족했다. 링크가 자리를 다 먹는 게 원인
  (URL 하나 180자). 링크 없는 날짜 줄은 25자뿐이라 훨씬 많이 실을 수 있다.
  → `alerts.board_top_n: 8`을 상한으로 두되 **개수를 고정하지 않고 한 통에
    들어가는 만큼 자동으로 줄인다**. 실측 도시 8개에서 도시당 5개씩 3,650자.
    노선이 늘어도 길이 초과로 발송이 실패하지 않는다. 테스트로 고정.

- **v1.47 고정판(live board) — 시세 확인을 푸시에서 분리**
  문제 제기(사용자): "하루 한 번 세 개씩이 최선인가? 실시간성·편의·한눈에 보기를
  다 만족하나?" → 아니었다. 받는 목적이 둘인데 둘 다 푸시로 처리하고 있었다.
  · **놓치지 않기** = 진짜 특가 → 푸시가 맞다 (그대로 유지)
  · **지금 판 보기** = 훑어보기 → 푸시는 잘못된 도구다. 내가 원하는 시점이
    아니라 시스템이 정한 시점에 오고, 하루만 지나도 낡고, 안 볼 때도 울린다.

  **해결**: 텔레그램 봇은 자기 메시지를 나중에 수정할 수 있고 **수정은 알림이
  울리지 않는다.** 방에 한 통 고정해두고 실행마다 내용만 갈아끼운다.
  · `notify.upsert_board` — message_id를 `meta.board_ids`에 보관하고
    editMessageText로 갱신. 지워졌으면 새로 보내고 id 갱신. 내용이 같으면 생략.
  · `notify.format_board` — 압축 포맷. 도시별 최저 1건만 링크(180자짜리 URL을
    전부 걸면 4096자를 넘긴다), 나머지 날짜는 `다른 날 9/16 130,200`로 한 줄.
    실측 2,539자로 한 통에 들어간다. **테스트로 길이 상한을 고정.**
  · `alerts.live_board: true`, 실행마다 갱신 (DRY_RUN 제외)
  · `alerts.cycle_report`를 **off로 기본 변경** — 고정판이 그 역할을 대신한다.
    아침 푸시로도 받고 싶으면 daily 로 되돌리면 된다.
  · 사용법: 처음 오는 📌 메시지를 길게 눌러 **고정(핀)** 하면 끝.

- **v1.46 다이제스트 개편 · 발송 시각 · 4096자 초과 버그**
  1. **도시별 1개 → 3개** (`alerts.digest_top_n`). 1개만 보면 "어느 도시가
     싼지"는 알아도 "언제 가야 싼지"를 모른다. 도시명을 제목으로 올리고
     날짜를 밑에 나열하는 형식으로 바꿔 반복을 없앴다.
  2. **발송 시각이 새벽 1시였다.** '한국 날짜가 바뀐 뒤 첫 완주'라 자정 직후에
     울렸다. → `alerts.digest_hour: 9` — KST 9시 이후 첫 완주에 발송.
  3. **4096자 초과로 발송 실패할 뻔했다.** 8개 도시 × 3날짜 = 6,400자.
     링크 URL 하나가 180자쯤이라 24개면 그것만 4,300자다.
     → 도시 블록 단위로 나눠 여러 통 전송(현재 2통, 각 3,200자대).
     `format_digest`가 문자열 대신 **리스트**를 반환하도록 변경.
     테스트로 제한 초과와 블록 쪼개짐을 모두 고정.

- **v1.45 '지금 최저가' 다이제스트** (사용자 지적으로 발견한 구조적 빈틈)
  문제: 한 번 알린 조합은 더 싸지기 전엔 다시 알리지 않는다(중복 억제).
  그래서 조용한 날에 "지금 뭐가 제일 싸지?"를 확인할 창구가 없었다.
  기존 '한 바퀴 완료' 메시지는 기준가 숫자만 나열해 날짜도 링크도 없어 무용지물.
  → `notify.format_digest`: **도시별 실제 최저 조합**을 날짜·시각·항공사·공항·
    링크까지 한 줄로. 싼 도시부터 정렬.
  · 하루 1회 한 바퀴 완료 시 자동 발송 (`cycle_report` 정책 따름)
  · **`DRY_RUN=digest`로 아무 때나 요청 가능** (한 통만, 저장 없음)
  · 실행 모드가 4가지가 됨: (빈칸) 실전 / `1` 테스트 / `preview` 미리보기 /
    `digest` 시세 요약

- **v1.44 제목 금액 오류 · 근처 날짜 중복**
  1. **제목이 실제 최저가와 달랐다.** `1인 368,688원부터`인데 바로 아래 근처
     날짜에 282,139원이 있었다. 제목을 알림 항목만 보고 정한 탓. 근처 날짜에는
     상한만 있고 하한이 없어 알림보다 싼 항목이 들어올 수 있다.
     → 제목 금액을 **메시지에 실제로 실리는 것 중 최저**로 계산.
  2. **같은 날 같은 값이 3박·4박 두 줄로** 떴다. → 같은 출발일·같은 가격·같은
     시각이면 박 수가 긴 쪽만 남긴다(길수록 이득).
  둘 다 테스트로 고정.

- **v1.43 항공사명을 IATA 코드 기준으로 매핑**
  `Trinity Airways`가 36건 잡혀 조사한 결과 **티웨이항공의 새 사명**이었다.
  대명소노그룹 인수 후 트리니티항공으로 변경 중이며, 승인 절차가 끝날 때까지는
  예매·운항이 기존 티웨이 이름과 코드(TW)로 유지된다. 구글은 이미 새 이름 사용.
  → 이름 대신 **코드로 매핑**(TW→티웨이)해 사명 변경에 흔들리지 않게 했다.
    코드가 없을 때만 이름 보조 매핑을 쓴다.

### 2026-07-26 03:25 실행 스냅샷 (v1.42 반영 후)
- 732건 검색 · 실패 0 · 콤보 **1,379개** (같은 공항 881 · 교차 498)
- 누적 알림 147건, 그중 **교차 조합 22건**
- 도시별 최저(1인): 제주 130,200 · 오사카 212,603 · 후쿠오카 255,877 ·
  나고야 282,139 · 도쿄 296,321 · 삿포로 409,492 · 가고시마 415,739 · 오키나와 525,571
- 도쿄가 하네다·나리타 한 도시로 묶여 비교됨 (city_groups 정상 작동)
- 나고야 최저는 v1.40 넓힌 창 덕에 김포 왕복(15:00 귀국 ⚠)이 차지했고,
  교차 조합(19:00 귀국)은 그보다 5만원 비싸게 나란히 표시된다 — 가격과 시간을
  사용자가 직접 저울질할 수 있는 상태

- **v1.42 교차 조합 검증에서 나온 4건 수정**
  1. **교차 조합에 왕복 검증이 잘못 붙었다** — `search_roundtrip(c.route.origin,
     c.route.destination, ...)`가 김포↔나고야 왕복을 조회했는데 실제 여정은
     김포→나고야 / 나고야→인천이다. 다른 상품 가격을 "왕복권 X원"으로 표시할
     뻔했다. → 교차 조합은 왕복 검증 대상에서 제외.
  2. **약한 알림이 61%였다** — 실측 알림 90건 중 55건이 기준가 대비 1% 미만.
     특가가 아닌데 알림이 가면 진짜 특가가 묻힌다.
     → `alerts.min_below_baseline_pct: 2` 신설. 역대 최저는 이 조건과 무관.
     **결과: 알림 90 → 43건, 약한 알림 55 → 0건, 메시지 11 → 5개.**
  3. **교차 조합의 네이버 링크 제거** — 다구간 URL 규격이 없어 엉뚱한 왕복
     검색으로 보내고 있었다. 구글 multi-city 링크만 남긴다.
  4. **항공사명 한글화** — `Peach Aviation/Jin Air` → `피치/진에어`.
  + 새로 생긴 조합은 깰 이전 기록이 없으므로 🏆 대신
    `🆕 이번에 새로 찾은 조합 · N월 현재 최저가`로 표기.

- **v1.41 인천/김포 교차 조합** (사용자 제안)
  서울 사람에게 인천·김포는 바꿔 쓸 수 있는 공항이다. `김포→나고야 / 나고야→인천`
  같은 조합을 만든다. **추가 검색 0건** — 네 방향 다리를 이미 다 수집하므로
  짝만 다르게 지으면 된다.

  **실측 이득 (2026-07-26 데이터로 검증 후 구현)**
  273개 도시·날짜 중 33건에서 교차가 최저. 최대 절감 1인 **184,600원**.
  | 출발일 | 박 | 기존 최저 | 교차 최저 | 절감 |
  |---|---|---:|---:|---:|
  | 9/3 | 3박 | 572,782 | 388,182 | -184,600 |
  | 8/22 | 4박 | 521,559 | 336,959 | -184,600 |
  | 8/27 | 4박 | 521,559 | 372,959 | -148,600 |
  전부 `GMP→NGO / NGO→ICN`. 김포발 피치가 싼데 NGO→GMP는 18시 이후 편이 없어,
  같은 공항끼리만 묶으면 그 싼 가는 편이 통째로 버려졌다.
  **교차하면 가격도 싸고 귀국 시각도 선호대로(18시 이후) 맞는다.**

  **구현**
  · `Combo.ret_route` 추가. 같은 노선이면 None, 교차면 오는 편 노선.
    `is_cross`는 방향 비교가 아니라 `ret_route is not None`으로 판정한다 —
    ret leg는 노선 키 기준 저장이라 실제 비행 방향이 반대다(GMP-NGO의 ret =
    NGO→GMP). 이걸 헷갈려 처음에 전부 교차로 잡히는 버그가 있었다.
  · `key`·`unit`을 분리해 같은 공항 조합과 기준가가 섞이지 않게 함
    (교차 unit은 `X:NGO-ICN|2026-08` 형태).
  · `search.cross_airports: true`, `search.city_groups`(도쿄=[HND,NRT])로
    하네다 입국 → 나리타 출국 같은 도시 내 공항 교차도 지원.
  · **메시지를 노선이 아니라 도시 단위로 묶는다.** 인천발·김포발·교차가 따로
    나가면 어느 게 싼지 비교가 안 된다. 제목은 `나고야 1인 335,860원부터`.
  · **공항을 반드시 표시한다** — `⇄ 김포 출발 / 인천 귀국` 또는 `인천 왕복`.
    공항을 헷갈리면 비행기를 놓치므로 안전 문제로 취급. 테스트로 고정.
  · 링크는 교차일 때 구글 multi-city 쿼리로 생성.
  · 콤보 778 → **1,107개** (교차 329개 추가).
- **v1.40 김포-나고야 오는 편 넓힌 창** (`data/time_hist.json` 전수 점검 결과)
  2026-07-26 02:44 실행(709건·실패 0) 데이터로 22개 노선·방향을 전부 확인:

  | 노선·방향 | 선호창 안 | 조치 |
  |---|---|---|
  | 김포-나고야 오는편 | **0/70 (0%)** — 8시×35 14시×3 15시×32 | **14시~로 넓힘** |
  | 인천-가고시마 가는편 | 1/38 (3%) | 이미 넓힘 (유지) |
  | 인천-삿포로 오는편 | 15/196 (8%) | 이미 넓힘 (유지) |
  | 김포-오사카 오는편 | 40/187 (21%) | 경계선이나 40편이면 충분 — 관찰만 |
  | 나머지 18개 | 29~92% | 조치 불필요 |

  → **넓힌 창 3곳 모두 데이터 근거 확보.** 기준(선호창 20% 미만)이 잘 작동한다.

### 2026-07-26 02:44 실행 스냅샷 (노선 11개, 콤보 778, 누적 알림 116건)
| 노선 | 콤보 | 최저(1인) | 노선 | 콤보 | 최저(1인) |
|---|---:|---:|---|---:|---:|
| 인천-나리타 | 99 | 296,321 | 인천-나고야 | 77 | 354,324 |
| 인천-오사카 | 97 | **212,603** | 인천-가고시마 | 35 | 415,739 |
| 김포-하네다 | 94 | 887,379 | 김포-오사카 | 27 | 319,333 |
| 인천-후쿠오카 | 91 | 255,877 | 김포-나고야 | 0→ | (v1.40에서 해결) |
| 인천-오키나와 | 89 | 525,571 | 김포-제주 | 81 | 130,200 |
| 인천-삿포로 | 88 | 409,492 | | | |
- 인천-오사카가 최저(21만원대). 김포-오사카는 32만원대로 50% 비싸다 — 공급이
  적고 도심 접근성 프리미엄이 붙는 노선 특성과 일치.
- 김포-하네다 88만원대는 일본발 편도가가 비싼 탓이 크다(왕복 검증으로 완화됨).

- **v1.39 김포-나고야 추가 — 출발공항 짝 전수 점검 완료**
  같은 목적지에 인천/김포 짝이 빠진 게 없는지 전부 확인했다. **재조사 불필요.**

  | 목적지 | 인천발 | 김포발 | 근거 |
  |---|---|---|---|
  | 오사카 간사이 | ✅ | ✅ v1.38 | 대한·아시아나·제주 6데일리 + 피치 4왕복 |
  | 나고야 주부 | ✅ | ✅ **v1.39** | 피치가 일본측 김포-오사카/나고야 운수권 독점 |
  | 도쿄 나리타 | ✅ | **없음** | 인천공항 개항 이후 운항 이력 자체가 없음 |
  | 도쿄 하네다 | 불가 | ✅ | 아래 설명 |
  | 삿포로·오키나와·가고시마·후쿠오카 | ✅ | **없음** | 김포 국제선 일본 취항지는 도쿄·오사카·나고야 3곳뿐 |
  | 제주 | 시범편만 | ✅ | 인천-제주는 주 2회 시범 운항(8월까지) |

  **인천-하네다는 영구 제외**: 야간 전용 노선이다. 밤에 인천 출발 → 새벽 하네다
  도착, 복편은 새벽 1~2시 하네다 출발. "가는 편 06~13시" 조건에 원천적으로
  걸릴 수 없다. v1.10의 "경유편만 반환" 판정도 실은 이것 때문이었고 파서
  문제가 아니었다. 시간창을 바꾸지 않는 한 켤 이유가 없다.

  1회 실행 732건 ≈ 9분 (제한 75분), 한 바퀴 2시간.
- **v1.38 메시지 정렬 통합 · 김포-오사카 추가**
  · **정렬 버그**: 근처 날짜 상한이 '가장 싼 주 항목 +10%'라 두 번째 주 항목보다
    근처 날짜가 쌌다. 실측 김포-제주에서 13만 → 16만 → 13만 순으로 보였다.
    → 주 항목과 근처 날짜를 **하나의 오름차순 스트림**으로 합쳤다.
      주 항목은 블록, 근처 날짜는 `·` 한 줄. 맨 아래 각주로 구분 설명.
  · **제목의 월 표기 오류**: 한 메시지에 8월·10월이 섞이는데 제목은 "8월"이라
    했다. → 제목은 `노선 1인 N원부터`로 바꾸고, 월별 근거는 **항목마다** 표시.
  · `similar_margin_pct` 10 → 15. 10%로는 후보가 1~2개뿐인 노선이 많았다
    (후쿠오카 1개). 근처 날짜 개수가 노선마다 다른 건 이 임계 때문이다.
  · **김포-오사카(GMP-KIX) 추가** — 그냥 빠뜨렸던 노선. 대한·아시아나·제주항공
    6데일리 + 피치 4왕복으로 공급이 충분하다. 노선 9개 → 10개.
- **v1.37 로그 소음 차단**
  primp/httpx 등이 요청마다 수백 글자 tfs URL을 통째로 찍어, 432 leg 실행이면
  그런 줄만 600개가 넘었다. 로그가 수 MB로 불어나 **복사·전달이 실패**했고
  (사용자가 붙여넣은 로그가 5회 연속 빈 내용으로 도착) 정작 필요한 통계·진단
  줄이 묻혔다. 해당 로거들을 WARNING으로 낮춤. 오류는 그대로 보인다.

### 실측 시간 분포 (2026-07-26 02:16 실행, `data/time_hist.json`, 탈락편 포함)
선호창: 가는 편 06~13시 / 오는 편 18시~
| 노선 | 가는 편 선호창 | 오는 편 선호창 | 판단 |
|---|---|---|---|
| **인천-가고시마** | **0/18 (0%)** — 전부 16시 | 18/19 (95%) | 가는 편 넓힘 **필수** |
| **인천-삿포로** | 98/106 (92%) | **10/102 (10%)** → 넓힘 시 81% | 오는 편 넓힘 **필수** |
| 인천-오키나와 | 77% | 49% | 넓힘 불필요 |
| 인천-후쿠오카 | 67% | 31% | 넓힘 불필요 |
| 인천-나고야 | 60% | 35% | 넓힘 불필요 |
| 김포-제주 | 58% (표본 1,514편) | 31% | 넓힘 불필요 |
| 인천-오사카 | 55% | 29% | 넓힘 불필요 |
| 인천-나리타 | 55% | 36% | 넓힘 불필요 |
| 김포-하네다 | 45% | 37% | 넓힘 불필요 |
→ **현재 두 개의 넓힌 창은 데이터로 정당성이 확인됐다.** 다른 노선은 불필요.
  **새 노선 판단 기준 (v1.44 기준 정련)**: 비율만 보지 말 것.
  김포-오사카 오는 편은 20%로 낮지만 표본 874편 중 175편이 선호창 안이라
  콤보가 충분히 생긴다(27개). → **절대 편수가 사실상 0에 가까워 콤보가 안
  생길 때만** 넓힌다. 현재 넓힌 3곳은 0%·2%·8%로 모두 그 경우였다.

- **v1.36 시간 분포를 파일로 누적 (`data/time_hist.json`)**
  · 이 값은 로그에만 있어 옮겨 보기가 번번이 실패했다. 저장소 파일로 두면
    `raw.githubusercontent.com/.../data/time_hist.json` 으로 언제든 확인 가능.
  · 실행마다 누적되므로 표본이 커진다. 초기화하려면 파일을 지우면 된다.
  · 형식: `{"ICN-KOJ": {"8": 1, "16": 18}, "_runs": 2, "_updated": "..."}`

### 노선별 시각 분포 실측 (2026-07-26 data/legs.json 기준, 채택된 편)
| 노선 | 가는 편 | 오는 편 | 판단 |
|---|---|---|---|
| 가고시마 | 08시×1 **16시×13** | 18시×11 19시×7 | 가는 편 넓힘 유지 (선호 7%) |
| 삿포로 | 07시×21 10시×3 12시×3 | **14시×6 16시×10** 20시×2 | 오는 편 넓힘 유지 (선호 11%) |
| 제주 | 06시×12 07시×2 | 19시×3 20시×7 21시×12 | 넓힘 불필요 |
| 오사카 | 07시×15 | 18시×13 19시×12 | 넓힘 불필요 |
| 후쿠오카 | 07시×17 08시×3 | 19시×11 20시×7 | 넓힘 불필요 |
| 나리타 | 08시×17 07시×8 | 18시×13 19시×11 | 넓힘 불필요 |
| 나고야·하네다·오키나와 | 07~09시 집중 | 18~20시 집중 | 넓힘 불필요 |
※ 넓히지 않은 노선의 "선호창 100%"는 검색 범위가 곧 선호창이라 당연한 결과다.
  시간창 밖에 더 싼 편이 있었는지는 `data/time_hist.json`(탈락편 포함)으로 볼 것.

- **v1.35 메시지 다듬기 + 다중 수신자**
  · D-day 삭제 (본문·근처 날짜 모두). 필요해지면 되살릴 것.
  · 근처 날짜에 **출발/귀국 시각과 항공사** 추가 —
    `· 8/19(수)~8/22(토) 3박 · 08:10/19:05 제주항공 · 480,000원/인`
  · 선호 시간 밖 표시를 긴 설명 줄에서 **시각 옆 ⚠ 한 글자**로 축약
    (`16:20⚠ 출발 · 18:55 귀국`). 매번 같은 문장이 반복돼 길기만 했다.
  · `TELEGRAM_CHAT_ID`에 콤마로 여러 대상 지정 가능.
    개인 여러 명이든 그룹(chat_id 음수)이든 동일하게 동작. 하나라도 성공하면 OK.
- **v1.33~34 시간창 override 규칙 확정 — 재론하지 말 것**
  경위: v1.32까지 override가 선호 창을 통째로 덮어써서, 넓힌 창 안 최저가를
  고르느라 사용자의 오전 선호를 조용히 어겼다(표시도 없었다). v1.33에서
  "선호 창이 비었을 때만 양보"로 바꿨으나 과교정이었고, v1.34에서 확정.

  **판단 근거 (2026-07-26 data/legs.json 실측)**
  | 노선·방향 | 채택된 출발 시각 | 선호 창 안 |
  |---|---|---|
  | ICN→KOJ 가는 편 | 08시 1건 · **16시 13건** | 1/14 (7%) |
  | CTS→ICN 오는 편 | 14시 6건 · **16시 10건** · 20시 2건 | 2/18 (11%) |

  **확정 규칙**
  · 넓힌 창이 있는 노선(KOJ 가는 편, CTS 오는 편)은 **넓힌 창 안에서 최저가**를
    고른다. 드물게 있는 선호 시간 편을 우선하지 않는다 — 애초에 "이 노선엔
    선호 시간 편이 없다"고 인정해 넓힌 것이므로 다시 오전 우선을 씌우면
    모순이고, 비싼 오전 편을 '특가'라며 알리게 된다.
  · 채택된 편이 선호 시간 밖이면 알림에 **반드시 표시**한다.
    `⚠️ 가는 편이 선호 시간대 밖입니다 (이 노선은 선호 시간대 운항이 거의 없어요)`
    원래 문제였던 '몰래 어김'은 이 표시로 해소된다.
  · 넓힌 창이 없는 나머지 7개 노선은 선호 창이 절대 기준 (변화 없음).
  · `Settings.window_for()` = 선호 창(항상 전역값),
    `Settings.fallback_window_for()` = 넓힌 창(검색 범위).
  · 새 노선에 넓힌 창을 줄지는 실행 로그의 "시간 분포" 줄로 판단할 것.
    선호 창 비율이 낮으면(대략 20% 미만) 부여 검토.

- **v1.32 시간 분포 집계 · 샤드 3→2**
  · 실행 끝에 노선·방향별 직항 출발 시각 히스토그램을 로그로 남긴다.
    시간창에 걸려 탈락한 편까지 세므로 **시간창 override의 정당성을 데이터로
    판단**할 수 있다. 예: `GMP-CJU(가는편) 창 06~13시 · 15/36편(42%) ·
    06시:2 07시:5 08시:4 … 19시:6 20시:5`
  · 샤드 3→2. 동시검색 이후 실측 분당 78건이라 1회 7분, 한 바퀴 2시간.
    샤드 1(한 바퀴 1시간)도 시간상 가능하나 하루 요청이 1만→2.4만 건으로
    늘고 가격이 시간 단위로 움직이진 않아 보류. config 주석에 계산표 있음.
- **v1.31 근처 날짜에 링크 · RTVERIFY 부호 버그**
  · 근처 날짜 목록의 날짜 자체를 구글 링크로. 요일·D-day·1인당 금액을 한 줄에.
    목록만 보고는 실제로 갈 수가 없어 정보로만 남던 문제 (사용자 지적).
  · RTVERIFY 로그가 `(-59% 낮음)`처럼 부호와 표현이 어긋나 있었다.
    v1.13에서 사전점검 경로만 고치고 본 경로를 놓쳤던 것.
    → `편도2장 X / 왕복티켓 Y (왕복이 +59%)`

### v1.30 실측 결과 (2026-07-26 01:00, 동시 검색 3)
| | 직전 | v1.30 |
|---|---|---|
| 실행 시간 | 26분 | **5분 36초** |
| 검색량 | 205건 | **432건** |
| 실패 | 0 | **0** |
| 콤보 | 138 | **493** |
노선별 가격확보: CTS 52 · HND 48 · FUK 47 · CJU 46 · OKA 46 · KIX 44 · NGO 41
· NRT 38 · KOJ 21. **한 바퀴 20분 이내.** 동시 3으로 차단 징후 없음.
남은 약점: ICN-KOJ 조건불일치 24, GMP-CJU/CJU-GMP도 시간창 탈락이 잦다
(`항목 21개 전원 탈락 (시간창밖)` — 하루 20편 넘게 잡히나 오전 편이 없는 날).
시간창은 사용자 확정 사항이라 조정하지 않는다.

- **v1.30 동시 검색으로 실행 시간 1/3**
  · `schedule.concurrency: 3` 신설. 네트워크 작업만 스레드로 돌리고 상태 기록은
    메인 스레드가 순차 처리 → 자료구조 경합 없음.
    요청 간 대기는 작업자마다 그대로 지켜져 초당 요청은 3/1.5 ≈ 2건 수준.
    실측 454 leg 기준 26분 → **약 9분** 예상. 실패가 늘면 2나 1로 낮출 것.
  · 폴백 경로 재시도를 1회로 제한. v1.24 관대 파서 도입 후 직항 쿼리 성공률이
    올라 이 경로 자체가 드물어졌는데, 같은 쿼리를 3초 대기 후 반복하고 있었다.
  · **preview 모드 부작용 없음 재확인**: 상태 저장(`state.save`)·한 바퀴 보고·
    관측 리포트·실패 알림 전부 preview의 `return 0` 뒤에 있다. 워크플로우의
    `git add data/`도 변경분이 없어 "변경 없음"으로 끝난다.
- **v1.29 알림 메시지 소비자 관점 재설계** (사용자 요청으로 UX 점검 후 개편)
  발견한 문제와 조치:
  1. 제목에 금액이 없어 잠금화면에서 열어봐야 알았다 → `노선 1인 313,400원`
  2. '기준가' 같은 내부 용어 노출 → `8월 요즘 최저가(410,000원)보다 8% 쌉니다`
  3. 1인당·총액이 항목마다 4개 숫자로 늘어짐 → 실제 낼 금액만 굵게,
     대안 구매법은 아래 한 줄로 이유와 함께
  4. 출발까지 남은 일수가 없어 급한지 판단 불가 → `D-17`
  5. `🆕 이번이 처음` 배지는 첫 알림에 거의 항상 붙어 정보량 0 → 삭제,
     재알림(🔻)만 표시
  6. 같은 항공사인데 가는 편·오는 편에 두 번 표기 → 한 번으로
  7. **근처 날짜 목록 기준 불일치(버그)**: 임계를 대표 금액(왕복일 수 있음)으로
     잡고 비교는 편도 2장과 해서, 왕복이 쌀 때 목록이 거의 안 떴다.
     편도 2장끼리 비교하도록 수정 + 라벨에 기준 명시
- **v1.28 한 바퀴 진행도·완료 보고**
  · 매 실행 로그에 `이번 바퀴 진행 2/3` 표시. 완주 시 `(완주)` 표기.
  · 완주하면 텔레그램으로 `🔄 전체 한 바퀴 완료` + 노선·월별 현재 최저가(1인당).
  · `alerts.cycle_report`: `daily`(기본, 하루 첫 완주만) | `every` | `off`
  · 완주 판정은 카운트가 아니라 **본 샤드 집합**으로 한다. 실행이 생략·중복돼도
    같은 샤드 반복을 완주로 오인하지 않는다 (테스트로 고정).
- **v1.27 한 바퀴 6시간 → 3시간, 묶음 간격 필터**
  · `shards: 6 → 3`. 실측 leg당 3.4초 · 전체 1017 leg 기준
    1회 실행 454건 ≈ 26분(제한 75분), 한 바퀴 3시간.
    샤드 2(한 바퀴 2시간·1회 31분)도 가능하나 GitHub 예약 실행이 자주
    지연·생략돼 한 번 놓쳤을 때 손실이 커서 3을 택함.
  · `alerts.bundle_min_gap_pct: 3` 신설 — 묶음 안에서 바로 앞 항목보다 3% 이상
    비싸야 별도 줄로 노출. 1~2% 차이 옆 날짜가 3줄로 늘어서던 것을 정리.
    빠진 날짜는 아래 '유사 가격대(+10% 이내)' 목록에 그대로 나온다.
  · 부수 효과: 왕복 검증은 여전히 상위 3개에 하므로 표시 항목 ⊆ 검증 항목.
    검증 안 받은 항목이 표시되는 일은 없다 (테스트로 고정).
- **v1.26 신규/하락 배지 + 재알림 최소 하락폭**
  · 항목마다 `🆕 이 날짜 조합은 이번이 처음` 또는
    `🔻 지난 알림 ₩X/인에서 ₩Y/인 더 내림 (-N%)`을 표시.
    Alert에 `prev_sent`(직전 알림가)를 실어 전달.
  · `alerts.min_redrop_pct`(기본 2%) 신설 — 직전 알림가보다 그만큼 더 싸져야
    재알림. 예전엔 1원만 내려도 다시 보내 같은 조합이 반복 노출됐다.
- **v1.25 정렬 기준 수정** — 노선 안에서 감지 지표(편도 2장)로 정렬하고 있었는데
  화면에 강조되는 금액은 편도·왕복 중 싼 쪽이라, 왕복이 싼 항목이 뒤로 밀려
  보이는 숫자가 뒤죽박죽이었다. 선별은 그대로 감지 지표 기준(왕복 검증 대상과
  일치시키기 위해), **표시 순서만 실제 강조 금액 오름차순**으로 재정렬.
  노선별 메시지도 가장 싼 노선부터 전송한다.
- **v1.24 응답 전 구획 수집** — 기본 파서가 `payload[3][0]`(추천 항공편) 한
  구획만 읽어 "그 외 항공편"을 통째로 버리고 있었다. 김포-제주에서 13편만
  잡히고 그마저 전부 시간창 밖이던 원인. 관대 파서가 모든 구획을 긁고
  중복을 제거한다. 또한 `_run_query`를 요청 1회 → 두 파서 병행 구조로 바꿔
  실패 시 중복 요청도 없앴다. 로그에 `TOLERANT 구획 추가 수집: 기본 N → 관대 M`
- **v1.23 1인당 금액 표기 · 링크에 항공사 필터 · 나리타/김포제주 시험 투입**
  · 금액: `₩377,060/인 (총 ₩754,121)` 형태. 관측 리포트·유사가격도 1인당
  · 링크: 검색 때 수집한 IATA 코드(`carrier`)로 tfs에 항공사 필터를 걸어
    날짜+직항+해당 항공사+인원까지 좁힌 왕복 검색 결과로 랜딩.
    구글 tfs에는 시각 필터가 없어 시간까지는 못 넣는다(대신 후보가 1~2편으로 줄어듦).
    코드를 모르면 필터 없이 링크만 만들고 라벨을 '검색결과'로 표기.
- **v1.22 미리보기 모드(`preview`) + 관측 4일→2일 단축**
- **v1.21 오사카·후쿠오카 시험 부활**, 탐침 코드 제거
- **v1.20 관대 파서 도입** — 데이터없음 19→3, 콤보 108→116
- v1.19 구글 우회 탐침 → 6종 실패했으나 파서 취약점 발견
- v1.18 네이버 탐침 제거, 네이버 중단 확정
- v1.17 네이버 3차 탐침(primp) → 실패, 네이버 포기
- v1.16 네이버 2차 탐침 → IP 차단 아님 확인 (페이지 200, API만 503)
- v1.15 네이버 API 1차 탐침 → 후보 4종 전부 503 (차단 확인)
- **v1.14 편도 2장·왕복 티켓 병기, 싼 쪽 자동 강조** (노선별로 갈림을 실측 확인)
- v1.13 대표 금액을 편도 2장 합산으로 환원(표본 편향, v1.14에서 교정) (실측상 왕복보다 저렴),
  왕복가는 참고 병기 · 사전 점검을 노선별로 · RTDIAG 구조 로깅
- **v1.12.2 DRY_RUN 상태 저장 버그 수정** — 테스트 실행이 관측 리포트/알림을
  "보냈음"으로 기록해 실제 알림을 누락시키던 문제. 이제 dry는 저장하지 않음

## 확정된 검색 범위 — 재론하지 말 것 (2026-07-26 사용자 결정)
"더 싸게 찾는 무료 방법"으로 아래를 제안했고 **전부 현행 유지로 결정**됐다.
| 제안 | 결정 | 비고 |
|------|------|------|
| 출발 요일 제한 해제 (일·월·화 추가) | **유지** — 수·목·금·토만 | 후보 43% 증가를 포기 |
| 여행 일수 확대 (2박·5박 추가) | **유지** — 3박·4박만 | |
| 경유 1회 별도 알림 | **유지** — 직항만 | |
| 가는 편 시간창 완화 (2026-07-25 결정) | **유지** — 06~13시 | 도착 첫날 확보 우선 |

즉 검색 범위는 더 넓히지 않는다. 개선은 **같은 범위 안에서 정확도·누락 줄이기**
방향으로만 진행할 것 (관대 파서·전 구획 수집이 그 예).
아직 열려 있는 선택지: 왕복 검증 대상 확대(`verify_max_queries` 상향).
지금은 노선별 상위 3개만 왕복 조회하므로 4등 이하에서 왕복이 싼 조합은 놓친다.

## 사용자 컨텍스트
- 성인 2명, 2026-08-01~10-31, 3~4박, 가는 편 06~13시 / 오는 편 18시 이후, 직항
  (오전 출발은 확정 선호. 더 싼 오후편을 놓치더라도 유지하기로 결정)
- 출발 요일: 수·목·금·토만 (일·월·화 제외 설정됨)
- 텔레그램 봇 사용 중 (발신 전용, Secrets 등록 완료)
- 사용자는 GitHub 웹 UI로 작업 (zip 압축 해제 후 전체 드래그 업로드 방식,
  .github 폴더는 숨김이라 워크플로우 변경 시 ✏️ 수동 교체 안내 필요)
- 알림에 유사가격(+10%) 대안 날짜 표시, 아침 다이제스트는 제외 결정
