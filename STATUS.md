# STATUS — 프로젝트 현재 상태 (인수인계 문서)

> 최종 갱신: 2026-07-25 (v1.7) · 새 대화에서 이어갈 때 이 문서와 SPEC.md를 먼저 볼 것

## 프로젝트 한 줄 요약
일본 7개 도시 + 제주 노선의 항공권 최저가를 GitHub Actions(공개 저장소
`hminhoz/flight-price-alert`)에서 2시간마다 감시, 특가 시 텔레그램 알림.
전체 설계·결정 근거는 SPEC.md 참고.

## 현재 상태: 첫 실전 가동 직전 (디버깅 중)

완료된 것:
- [x] 전체 코드 구현 및 로직 테스트 통과 (tests/test_pipeline.py)
- [x] 텔레그램 봇 생성, chat_id 확인, GitHub Secrets 등록
- [x] 저장소 업로드, 워크플로우 첫 실행 성공 (실행 #2, 44분)
- [x] typing_extensions 의존성 버그 수정 (requirements.txt에 추가)

진행 중 / 관찰 필요:
- [x] 실패율 진단 완료 (7/25 로그): GMP-CJU 전량 no-data(구글에 국내선 가격 없음
  → 네이버 소스 필요), ICN-TYO 전량 no-data(도시코드 문제 → v1.6에서 공항 분리),
  KIX·FUK no-data 집중(파서 이슈 추정 → v1.6 재시도로 관찰), ICN-CJU 실패 14건
  (에러 내용 미확인 → v1.6 에러 로깅으로 다음 실행에서 확인)
- [x] **가격확보 0건 원인 확정 (7/25)**: fast-flights가 구글 원본 시각을
  [시, 분] 배열 그대로 넘기는데 파서가 문자열만 처리 → 전 항공편이 시간창 탈락.
  v1.7에서 수정 (소스코드 분석으로 확정). 'no flights found' 예외도
  정상 신호로 재분류(재시도 제거), 항공사 코드 문자열 처리.
- [ ] **v1.7 적용 후 확인**: 가격확보 숫자가 살아나는지, 콤보 생성, 텔레그램
  수집 현황에 건수 표시, KIX·FUK·NRT 대형노선 no-data가 지속되는지
- [ ] 콤보 0개는 첫 실행의 정상 현상(가는/오는 편이 모두 수집돼야 생성).
  하루 뒤에도 0이면 이상.
- [ ] 원거리(출발 22일+) 하루 1회 체크의 충분성 — 가동 2주 후 data/의
  daily_min 히스토리로 실증 검증 예정

## 버전 이력
- v1.0 최초 구현 (왕복→편도 합산 설계 전환 포함)
- v1.1 가동 시작 메시지 + 관측 기간 일일 리포트
- v1.2 실측(8초/건) 반영: 샤드 12, 타임아웃 75분
- v1.3 3계층 체크(임박 5일=2시간마다 / 주시 21일=하루 2회 / 원거리=하루 1회)
       + 일별 체크 시각 회전
- v1.4 IndexError→no-data 분리(재시도 제거로 실행시간 단축), 노선별 통계 로깅
- v1.4.1 requirements.txt에 typing-extensions 누락 수정
- v1.5 알림에 '유사 가격대 다른 날짜' 섹션(+10% 이내 최대 4건),
       출발일 제외 설정(exclude_departures)
- v1.5.1 출발 요일 제외(exclude_weekdays) — 현재 일·월·화 출발 제외 설정됨
- v1.5.2 제외 요일 일·월·화로 확정
- v1.9 (7/25) DIAG 판독 반영: ① 폴백이 경유편만 반환 시 직항 쿼리 재시도
       (ICN-HND 회수 목적) ② 3차 폴백 = 한국 마켓(gl=KR) 요청 (KIX·FUK·NRT 가설)
       ③ 0시 null 표기 파싱 ④ 국내선은 구글 데이터 부재 확정 → 네이버 소스 필요
- v1.8 (7/25) 직항필터 쿼리 실패 시 무필터 재조회 폴백 (대형노선 KIX·FUK·NRT
       전멸 대응 시도), TypeError도 no-data 계열로 재분류
- v1.7 (7/25) 시각 파싱 근본 수정([시,분] 배열 대응) — 가격확보 0건 사태 해결,
       FlightsNotFound를 no-data로 재분류, 항공사 코드 표시 수정
- v1.6 (7/25 진단 반영) ① 샤드를 시각 기반→상태 커서 순환으로 교체
       (GitHub 스케줄 지연/생략에도 검색 누락 0) ② cron 1시간 간격
       ③ ICN-TYO를 ICN-NRT/ICN-HND로 분리(TYO 도시코드 전량 no-data)
       ④ IndexError 1회 재시도 ⑤ 노선별 마지막 에러 로깅 ⑥ 관측 4일

## 사용자 컨텍스트 (대화 이력 요약)
- 성인 2명, 2026-08-01~10-31 사이 3~4박, 가는 편 06~13시 / 오는 편 18시 이후, 직항
- 알림 채널: 텔레그램 (카카오는 토큰 유지보수 부담으로 제외)
- 아침 다이제스트는 검토 후 제외, 가격 그래프는 v2
- 노선 추가 의향 있음 (README "노선 추가" 절 참고, 7~8개 여유)
- 사용자는 git CLI 대신 GitHub 웹 UI로 작업 (파일 업로드/✏️ 편집 방식 안내 필요)
